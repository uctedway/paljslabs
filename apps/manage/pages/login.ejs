<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=1280, initial-scale=1.0">
  <title><%= title || '48LAB Manage Login' %></title>
  <link rel="stylesheet" href="/css/manage.css">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" />
</head>
<body>
  <main class="manage-login-wrap">
      <div class="manage-login-card">
        <h1 class="manage-login-title">48LAB</h1>
        <p class="manage-login-subtitle">Manage Console Login</p>
        <% if (errorMessage) { %>
          <p class="manage-alert error"><%= errorMessage %></p>
        <% } %>

        <form method="post" action="/manage/login" class="manage-form">
          <input type="hidden" name="next" value="<%= next || '/manage' %>">
          <div>
            <label class="manage-label" for="admin_id">관리자 아이디</label>
            <input id="admin_id" name="admin_id" type="text" class="manage-input" autocomplete="username" required>
          </div>
          <div>
            <label class="manage-label" for="password">비밀번호</label>
            <input id="password" name="password" type="password" class="manage-input" autocomplete="current-password" required>
          </div>
          <button type="submit" class="manage-submit">
            <span class="material-symbols-outlined" style="font-size:18px;">login</span>
            로그인
          </button>
        </form>
      </div>
    
  </main>
</body>
</html>
