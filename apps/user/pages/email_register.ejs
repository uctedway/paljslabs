<main class="login-page">
  <section class="login-hero">
    <div class="login-card login-card--email">
      <p class="login-kicker">48LAB ACCOUNT</p>
      <h1 class="login-title">이메일 회원가입</h1>
      <p class="login-lead">이메일 계정을 새로 만들어 시작하세요.</p>
      <% if (emailRegisterErrorMessage) { %>
        <p class="login-error"><%= emailRegisterErrorMessage %></p>
      <% } %>

      <form class="email-auth-form" action="/user/email-register" method="post">
        <% if (pendingReferralCode) { %>
          <input type="hidden" name="referral_code" value="<%= pendingReferralCode %>">
        <% } %>
        <label class="email-auth-label" for="email-register-id">이메일</label>
        <input id="email-register-id" class="email-auth-input" type="email" name="email" placeholder="you@example.com" autocomplete="email" value="<%= emailPrefill || '' %>">

        <label class="email-auth-label" for="email-register-name">이름</label>
        <input id="email-register-name" class="email-auth-input" type="text" name="name" placeholder="이름 입력" autocomplete="name" value="<%= namePrefill || '' %>">

        <label class="email-auth-label" for="email-register-password">비밀번호</label>
        <input id="email-register-password" class="email-auth-input" type="password" name="password" placeholder="비밀번호 입력" autocomplete="new-password">

        <label class="email-auth-label" for="email-register-password-confirm">비밀번호 확인</label>
        <input id="email-register-password-confirm" class="email-auth-input" type="password" name="password_confirm" placeholder="비밀번호 재입력" autocomplete="new-password">

        <button type="submit" class="email-auth-submit">회원가입</button>
      </form>

      <p class="login-subtext">
        이미 이메일 계정이 있으신가요? <a href="/user/email-login">이메일 로그인</a>
      </p>
    </div>
  </section>
</main>
