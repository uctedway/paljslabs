<main class="login-page">
  <section class="login-hero">
    <div class="login-card login-card--email">
      <p class="login-kicker">48LAB ACCOUNT</p>
      <h1 class="login-title">이메일 회원가입</h1>
      <p class="login-lead">이메일 계정을 새로 만들어 시작하세요.</p>
      <section class="signup-consent-section">
        <h2 class="signup-consent-title">회원가입 약관 동의</h2>
        <div class="signup-consent-grid">
          <div class="signup-consent-panel">
            <p class="signup-consent-panel-title">이용약관</p>
            <div class="signup-consent-scroll">
              <%- include('../../core/pages/partials/legal_terms_content') %>
            </div>
          </div>
          <div class="signup-consent-panel">
            <p class="signup-consent-panel-title">개인정보처리방침</p>
            <div class="signup-consent-scroll">
              <%- include('../../core/pages/partials/legal_privacy_content') %>
            </div>
          </div>
        </div>
      </section>

      <form class="email-auth-form" action="/user/email-register" method="post" novalidate>
        <% if (pendingReferralCode) { %>
          <input type="hidden" name="referral_code" value="<%= pendingReferralCode %>">
        <% } %>
        <label class="email-auth-label" for="email-register-id">이메일</label>
        <input id="email-register-id" class="email-auth-input" type="email" name="email" placeholder="you@example.com" autocomplete="email" required>

        <label class="email-auth-label" for="email-register-name">이름</label>
        <input id="email-register-name" class="email-auth-input" type="text" name="name" placeholder="이름 입력" autocomplete="name" required>

        <label class="email-auth-label" for="email-register-password">비밀번호</label>
        <input id="email-register-password" class="email-auth-input" type="password" name="password" placeholder="비밀번호 입력" autocomplete="new-password" required>

        <label class="email-auth-label" for="email-register-password-confirm">비밀번호 확인</label>
        <input id="email-register-password-confirm" class="email-auth-input" type="password" name="password_confirm" placeholder="비밀번호 재입력" autocomplete="new-password" required>
        <label class="signup-consent-check"><input type="checkbox" id="agree-terms" name="terms_agreed" value="1"> 이용약관에 동의합니다. (필수)</label>
        <label class="signup-consent-check"><input type="checkbox" id="agree-privacy" name="privacy_agreed" value="1"> 개인정보처리방침에 동의합니다. (필수)</label>

        <button type="submit" class="email-auth-submit">회원가입</button>
      </form>

      <p class="login-subtext">
        이미 이메일 계정이 있으신가요? <a href="/user/email-login">이메일 로그인</a>
      </p>

      <script type="module">
        import { bindEmailAuthForm } from '/js/modules/apps/user/email_auth_form.js';

        bindEmailAuthForm('.email-auth-form', [
          { selector: '#email-register-id', message: '이메일을 입력해주세요.' },
          { selector: '#email-register-name', message: '이름을 입력해주세요.' },
          { selector: '#email-register-password', message: '비밀번호를 입력해주세요.' },
          { selector: '#email-register-password-confirm', message: '비밀번호 확인을 입력해주세요.' },
          { selector: '#agree-terms', message: '이용약관에 동의해주세요.' },
          { selector: '#agree-privacy', message: '개인정보처리방침에 동의해주세요.' },
        ]);

      </script>
    </div>
  </section>
</main>
